<h1>{{title}} 🚀</h1>

<p>Get off that sofa and explore:</p>
<form action="" method="POST">
  <label for="district">Choose a district where you want to start:</label>
  <select id="district" name="district">
    {{#each enterValueHere}}
    <option value="{{this.enterValueHere}}">{{this.enterValueHere}}</option>
    {{/each}}
  </select>
  <br>
  <label for="distance">Distance:</label>
    <select id="distance" name="distance">
      <option value="">< 10km</option>
      <option value="">10 - 20km</option>
      <option value="">< 30km</option>
      <option value="">> 30km</option>
    </select>
  <br>
  <label for="mode">Mode:</label>
    <input type="radio" id="hike" name="hike" value="hike">
    <label for="hike">Hike</label>
    <input type="radio" id="bike" name="bike" value="bike">
    <label for="bike">Bike</label>
  <br>
  <button type="submit">Search</button>
</form>

{{#each trips}}
  <div>
    <h3>{{title}}</h3>
    <p>{{Address}}</p>
    <p>{{Distance}}km</p>
    <img src="{{Picture}}" alt="picture of trip">
    <a href="">Find out more</a>
  </div>
  <div id="map"></div>
{{/each}}


<a href='/signup'>Signup</a><br>
<a href='/login'>Login</a>

{{!-- need to add div with id="map" to render mapbox --}}
{{!-- Add mapbox token & create new map: here or do I need to do it in a specific js file? --}}
{{!-- starting position in new map should be the coordinates of tour start --}}
{{!-- set doubleClickZoom to true --}}
{{!-- Also need to set navigation thingy: check out screen shot for that--}}
{{!-- that sets the map but not yet the needle/marker! --}}
{{!-- Check out markers documentation --}}
{{!-- Coordinates that can change: Langitude & Latitude --}}
